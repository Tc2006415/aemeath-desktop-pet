# 分阶段必要测试门槛 v1

状态：供 PM 接受；本轮为文档收敛，所有运行测试均未执行。没有可运行桌宠、角色成品或真实来源通过结论。

## 依据与执行原则

运行方向以 PM 决策提交 `bf673927d3d88a6da071bfe49c3ec1aaa4fadb2b` 的 AGENTS、README、project-status 和 ADR 0001 为准：独立 Windows 11 x64，首版单个选定任务，未连接为 unknown/离线，模拟有明确标识，真实联动另验。不再以选择独立程序作为开放问题。

专业输入固定为 DEV `02626be7ab5ec947e702a9d85612e91045959e93`、ART `d54730bfde5fc666f93e3f3a4ce96298b8f946cb`、INT `4782cd5c5fd7d5c18a96526a89e8767caf080896`、QA `841021ebd13cbfbb4fe85c05f8a20f80b338160f`。前三份为对应的 docs/proposals/desktop-runtime.md、animation-spec.md、codex-integration.md；QA 输入为 [原矩阵](../proposals/acceptance-matrix.md) 和 [首稿评审](m1-proposal-review.md)。本文件只选取和分配既有 A/W/V/R/QAR 用例，不新增等价用例，也不重开已验收 001 评审。

本轮 DEV/ART/INT 的 002 新规格未纳入上述固定输入，均为**待集成复核**。PM 收齐并接受版本后，检查下表引用能否映射至实际字段、时钟、坐标、动作集合和命令；不能据本文件宣称新规格已独立评审。历史提案中尚未选方向的文字由新 ADR 覆盖，候选 5/15/30 秒、460 ms、画布和锚点不自动成为正式契约。

单一推荐：先过接口检查，再做诊断窗口；首包角色和模拟状态各有独立证据，可并行制作，进入日常使用前汇合。真实联动只在来源具备后开启。最终包分别记录独立版或含真实联动版的能力范围，不以独立版通过冒充真实联动通过。

- **必做**：该阶段对应能力交付前必须通过；未执行、无设备、无有效输入或无法判定均记“待验”，并阻塞该能力验收。发现实际缺陷记“失败”。阶段文档存在不算通过。
- **按改动触发**：列明的模块/契约变化发生时必须重跑；不触发时引用最近有效证据及未受影响理由。不能将从未执行的必做项当成未触发而跳过。
- **可延后**：不阻塞当前阶段；到明确的目标阶段或纳入支持范围时转为必做。无范围外的隐含支持承诺。
- 逻辑时间使用可控单调时钟，检查阈值前、等于、后；取正式契约最小时间步长，不靠长时间 sleep。动画时长、提示资格、业务有效期和连接活性分开断言。
- 窗口、鼠标捕获、DPI、屏幕拓扑、键盘/讲述人退出和打包启动必须有真实 Windows 应用证据；几何单测、截图概念图和浏览器设备模拟均不能替代。
- 程序/联动负责实现与自动化，动画负责帧检查；测试角色核对实际结果并复现关键手工路径，PM 接受阶段结果。用户保留最终造型与公开发布决定。

## 1. 接口：先使预期可判定

此阶段为文档/示例检查，不要求编写镜像文档的测试。D1–D7 为原矩阵决策项，不是新增用例。所有数值和错误分支必须能引用 PM 接受的契约；缺一项便只阻塞依赖它的实现，不无限扩大为重新选型。

| 分类 / 既有依据 | 检查责任 | 具体输入与操作 | 实际风险与可判定通过标准 | 证据 / 失败阻塞 |
| --- | --- | --- | --- | --- |
| 必做 / D1–D4、A02–A11、QAR01–QAR04 | 程序＋联动提出，测试核对，PM 接受 | 对接受版契约手工走读：拖动完成→release 中新轮次；到期；重复终态；心跳持续但业务不更新；检查一个合法模拟示例及一个拒绝示例 | 发现两层各自计时/去重、先消费漏播、旧状态复活。必须明确字段/轮次/代次、唯一状态归约与提示消费责任、计时起点和等号、release 重算、异常处置；示例能按契约判定，不冻结旧稿候选数值 | 契约提交、示例解析命令/退出码（有机器格式时）、逐项走读结论；缺项阻塞模拟状态相关实现 |
| 必做 / D6、A14–A16、QAR05/QAR07 | 程序＋动画提出，测试核对，PM 接受 | 检查最小素材清单/示例、源像素至窗口坐标换算示例、抓取点和固定锚点、错误回退 | 发现倍率重复换算、帧越界、坏回退帧致不可见窗口。须有唯一画布/导出倍率/锚点单位、时长、动作入口、读取格式/资源上限、DPI 策略、可达错误/退出路径 | 接受版素材及宿主契约、独立手算示例和语法检查记录；缺项阻塞加载/窗口相关实现 |
| 必做 / D7、W01/W08/W11 | 程序提出，测试核对，PM 接受 | 核对选定 SDK/宿主、构建运行方法、Windows 11 x64、实际退出路径、首批设备/DPI 支持表 | 发现无法构建或无法验证仍承诺可用。应明确命令、责任人、可用设备与未覆盖项；这里只确认可执行计划，不宣称工具链已安装或窗口通过 | 契约版本及设备覆盖表；缺命令/工程授权阻塞诊断实施，无设备阻塞对应支持验收 |

## 2. 诊断窗口：先证明能启动、拖动、退出

允许明确标记的诊断帧，不要求完整角色包或真实状态源。程序执行，测试在真实应用复核；所有下表行必做。

| 既有 ID | 输入 / 操作 | 实际故障与通过标准 | 证据 / 失败阻塞 |
| --- | --- | --- | --- |
| W01、W02、A03 | 从构建产物启动；深浅背景观察；慢/快拖、窗口外松手、捕获丢失、立即再抓 | 检出透明实色底、卡住拖动态、抢占鼠标。窗口真实可见、透明正常；所有结束路径退出拖动，可再次操作其他窗口；诊断片段不冒称角色动画 | 包/提交与启动记录、含按下松手/焦点变化的录屏；阻塞诊断窗口交付 |
| W08 | 角色失焦、被遮挡时，用键盘找到并执行退出；讲述人核对入口名称/焦点 | 检出只靠点小角色才能退出及残留进程。键盘路径可达、名称和焦点可识别，退出后进程/窗口及捕获释放 | 实际键盘路径、讲述人反馈、退出前后进程记录；阻塞诊断及后续可使用包 |
| A14、W09、QAR07 | 用测试副本制造缺图、坏图片、缺回退帧、越界裁切、非法时长和超过接受上限的资源；在真实应用至少复现坏图片＋坏回退帧组合 | 检出解码崩溃/忙等和完全透明“假成功”。非法素材拒绝或安全回退；回退也坏时可见报错、键盘可退出，受限资源不失控分配 | 自动输入/错误类别/命令结果，真实错误画面及退出记录；阻塞加载器和诊断交付。测试夹具不作角色素材 |
| A15 | 用两个循环、一个单次诊断片段，可控时钟走首尾并在结束边界中断/重启 | 检出末帧卡住、重复结束回调和旧回调覆盖。帧序/时长符合契约，单次结束最多一次，取消的旧片段不覆盖新片段 | 输入时间表、可观察帧/结束次数和测试报告；阻塞播放器交付 |

DPI/负坐标/屏幕变化若此时就宣称支持，必须提前执行第 5 节对应实机门槛；不能等日常使用阶段再补证据。未支持设备组合明确标待验。

## 3. 首包角色：验证实际交付的最小动作集合

首包范围由接受版素材契约确定；固定输入推荐 idle-smile（改编）与 idle-soft（原创），不是要求本轮生成它们，更不是强制先完成 47 帧。动画负责逐帧，程序负责加载，测试复核实机，用户认可造型。

| 分类 / ID | 输入 / 操作 | 实际故障与通过标准 | 证据 / 失败阻塞 |
| --- | --- | --- | --- |
| 必做 / V01–V03、A14/A15 | 加载实际首包；将角色与游戏像素参考对照；逐项核对已交付动作的进入、循环接缝或单次结束；检查源帧/时长/透明边缘及固定锚点 | 检出回退旧造型、灰底、裁切、漂移、接缝跳变及把改编说成实机复刻。每个交付动作至少完整播放一次；文件与契约一致；来源说明准确，获得造型认可。ART 的连续播放/间隔截图记录不证明逐帧循环 | 素材提交/校验值、帧检查记录、应用播放短录屏和用户造型反馈；失败阻塞首包角色验收，不否定已通过的诊断窗口 |
| 按改动触发 / V02、QAR05、W09 | 更改画布/锚点/导出倍率、透明规则或加载器时，重查受影响动作，并用一个代表动作跨约定 DPI；改错误路径时复现坏素材退出 | 防止旧诊断证据掩盖新包错位/损坏。受影响动作保持锚点与清晰度契约，退出仍可达；仅改表情像素且不改边界时，不重跑完整窗口套件 | 新旧版本差异、受影响动作记录与必要实机证据；失败阻塞该素材更新 |

复杂表演、机甲和未纳入首包的动作可延后；纳入交付时按同一 V02 验收，不为每帧每 DPI 新增等价用例。

## 4. 模拟状态：验证真实风险，不冒充真实联动

联动负责状态归约测试，程序负责表现协调，测试复核轨迹及 W03 真窗口。单个选定任务可以包含多轮次和任务切换，不开启多任务聚合。所有下表行必做；mock 与未连接标识手工核对即可，不给标识文案写单元测试。

| 既有 ID | 输入 / 操作 | 实际故障与通过标准 | 证据 / 失败阻塞 |
| --- | --- | --- | --- |
| A01、A07、A08、QAR03 | 六态有效序列；同一终态重复/乱序、序号缺口、同 ID 异载荷；新轮次 working 后旧轮次 completed；新连接代次与旧代消息 | 检出任务串线、旧完成覆盖新工作、冲突仍庆祝。有效转换符合接受契约；旧消息不回滚，异常按契约拒绝/unknown/重同步，历史快照不补庆祝 | 合成输入、预期与实际归约结果、拒绝类别、命令/退出码；失败阻塞模拟状态验收 |
| A05/A06/A10、QAR02/QAR03 | 同轮次终态在拖动遮挡前后重送；真正开播后中断、再次拖动/切任务并返回；重连快照含已完成轮次 | 检出提前消费导致漏提示或中断后无限重播。按 PM 接受的消费时点记录一次提示启动；重复和历史重放不得获得新提示；遮挡期间不得错误消费；新状态能够中断 | 提示资格与实际启动次数的轨迹、消费记录、自动结果；失败阻塞模拟提示验收 |
| A09–A11、QAR04 | 可控时钟分别跨连接时限、业务有效期、提示窗口；只持续 heartbeat 的对照组与权威快照续期组；壁钟跳变/休眠间隔 | 检出心跳把陈旧工作无限保鲜、壁钟变更误判和静默庆祝。阈值前/等于/后符合契约；心跳不续业务，失去可信性显示 unknown/离线；恢复需合法新基线 | 分离的逻辑时钟输入、业务/连接/提示输出与断言结果；失败阻塞模拟状态验收 |
| A02–A04/A15、QAR01/QAR02、W03 | dragging 收到 completed；release 中分别送新 working、过期、断连；release 中再次按下；补一条合法未过期完成路径 | 检出松手锁定旧状态、release 播完旧庆祝、卡住放下和吞掉合法提示。提示开始前按最新有效状态重算；无效/被取代不庆祝；合法资格按消费规则仅启动一次；再抓可立即恢复拖动 | 可控时钟覆盖各逻辑边界；真窗口至少录制 release 中新状态、到期、再抓及合法完成。记录注入时刻，不能靠录屏猜发生顺序；失败阻塞模拟与日常使用 |

以上策略细节若 002 契约尚未接受，记待集成复核/待验，不能自行挑选旧提案数值判通过。A05/A06 的异常序列与 QAR03 合并执行并关联多个 ID，不重复写同等测试。

## 5. 日常使用：位置、DPI 与恢复

程序在真实 Windows 执行，测试复核；以下首次交付日常使用能力必做，此后按对应模块变化重跑。

| ID / 触发模块 | 输入 / 操作 | 实际故障与通过标准 | 证据 / 失败阻塞 |
| --- | --- | --- | --- |
| A13、W07 / 持久化、窗口尺寸 | 保存位置/倍率后正常退出重启；强制终止重启；坏设置重启 | 检出设置丢失、启动即离屏、坏文件致无法退出。正常退出正确恢复；异常按契约安全回退到可达工作区；退出仍可用 | 临时测试配置、重启前后截图/值、自动结果及真机记录；阻塞日常使用 |
| A16、W04–W06、QAR05 / 坐标、DPI、显示器、素材倍率 | 一个代表动作，在支持表各 DPI 档分别查看/抓取；至少一组混合 DPI 跨屏往返与左侧或上方负坐标布局；静止和拖动时移除副屏；更改主屏/缩放/工作区并重启 | 检出重复缩放、鼠标锚点跳变、负数误判、移屏后永久不可见。按正式坐标策略保持锚点/抓取关系与可达窗口，尺寸/清晰度满足判据；没有实测的 DPI/拓扑不得列为已支持 | 独立几何期望值＋真实显示设置、操作录屏、误差记录。100/125/150/200% 为候选覆盖，实际以接受的支持表为准；缺设备必要项记待验，阻塞该支持声明与含该承诺的日常交付 |
| W10、W08 / 电源恢复、输入捕获、退出入口 | 工作/拖动中锁屏或睡眠，恢复后来源不可用；再键盘退出 | 检出恢复后捕获卡住、动画高速补播、重复进程及陈旧状态。恢复后状态按契约重同步/unknown，可重新操作且退出释放进程 | 时间/进程记录、恢复及退出短录屏；阻塞日常使用 |

不做动作×DPI×屏幕布局全组合：所有实际交付动作在主验收环境查一遍；几何边界选覆盖最宽羽饰/最大位移的代表动作，另外用固定锚点诊断帧测换算。跨屏只选能覆盖关键风险的混合 DPI 对；改变渲染/坐标规则后重跑相关代表路径。

## 6. 真实联动：单独的接入门槛

联动负责来源/权限证据和受控接入，程序负责展示，测试独立核对。没有正式实现派单前不接真实数据。缺受支持且仅状态的来源时记待验；不阻塞已通过的独立/模拟能力，也不能把真实联动标通过。

| 分类 / ID | 输入 / 操作 | 实际故障与通过标准 | 证据 / 失败阻塞 |
| --- | --- | --- | --- |
| 必做 / QAR06、R01 | 先核对受支持 Windows 入口、只读权限、在进入桌宠前的仅状态投影；具备且获派单后用无私密内容的真实受控任务触发开始、等待、继续、正常终态、失败 | 检出读取全文后声称仅状态、局部工具失败误报轮次失败、CLI 会话冒充 Desktop 订阅。六态按来源能力逐项证实；无法提供状态明确 unknown/限制，不自行豁免；完成只表示当前轮次结束 | 接口/来源版本、权限/投影证据、脱敏事件与桌宠同步记录；缺失阻塞对应真实能力验收 |
| 必做 / R02/R03、A10、QAR03/QAR06 | 真实连接断开、恢复并获得基线；来源支持的旧快照/重放；mock→real 切换及伪 real 拒绝 | 检出断连仍工作、历史完成重播、模拟基线污染真实状态。断连/过期显示未知，重连不补庆祝，切换清除旧基线、身份依据配置；不能自然产生的乱序由既有自动测试补测并明确模拟 | 真实源连接时间、接收轨迹及真窗口录屏；模拟补测另列，不冒充真实源异常；失败阻塞真实联动 |
| 按改动触发 / A01/A05–A11、QAR01–QAR04 | 适配层接入改变归约、有效期或提示边界时重跑第 4 节受影响的模拟轨迹和 W03 | 防止真实接入破坏已通过的拖动与时序。相同接受契约下行为保持，新增差异经 PM 接受并更新期望 | 差异范围与受影响测试结果；失败阻塞真实接入更新 |

R04/A12 的多任务聚合不属于首版；选定任务切换与旧轮次隔离仍由 A08/QAR02 检查，不因聚合延期而删去。

## 7. 最终包：验证将交付的确切产物

程序负责打包与干净环境验证，测试核对包与证据一致，PM 接受交付范围；公开发布另需用户授权。

| 分类 / ID | 输入 / 操作 | 实际故障与通过标准 | 证据 / 失败阻塞 |
| --- | --- | --- | --- |
| 必做 / W11、W01/W02/W08/W07 | 取确切发布候选包，在无开发 SDK/开发工具的 Windows 11 x64 干净环境安装或解压；启动、拖动、键盘退出、重启恢复；资源从包路径加载 | 检出开发目录/SDK 隐式依赖、打包漏资源、退出残留。无需源码或开发工具即可启动、使用和退出；设置可写，未擅自新增自启动或修改 Codex 安装文件 | 包 SHA-256、提交、环境及依赖清单、命令/退出码、启动至退出录屏与进程记录；失败阻塞最终包 |
| 必做 / V02、W09、W04–W06 | 确认包内素材与已验收哈希一致；对包再做一组坏素材退出；核对全部宣称支持的 DPI/拓扑证据是否适用当前构建 | 检出包内混入旧图/加载差异和超范围支持声明。坏素材仍可退出，素材版本对应验收记录；未变模块可引用同一代码的既有实机结果，打包改变 DPI manifest/启动宿主则必须重跑相关真机路径 | 资源哈希、错误/退出证据、支持表逐项引用与变更说明；失败或必要证据缺失阻塞最终包对应承诺 |
| 按改动触发 / W12 | 新渲染循环、计时器、事件突发处理或使用中出现资源增长/卡顿时，在同一设备观察稳定待机及连续操作 | 检出忙等、持续内存增长和明显卡顿。按 PM 接受预算判定；预算未定仅报告测量，不写“性能达标”；若故障影响使用则阻塞交付 | 采样工具/间隔/时长、CPU/内存趋势、复现操作和预算；未触发不要求专门性能跑分或跨框架比较 |

独立版最终包可将真实联动列为未提供/待验，并保留未知/模拟标识；需 PM 明确接受这个交付范围。含真实联动声明的最终包必须有第 6 节对应来源及版本的证据。真实来源暂无进展不是跳过必要窗口测试的理由。

## 可延后与回归选择

| 既有 ID / 内容 | 何时可延后 | 转为必做的触发点 |
| --- | --- | --- |
| A12/R04 多任务聚合 | 首版只有一个选定任务 | PM 纳入聚合范围；不能据此延后单任务切换/旧轮次隔离 |
| V02/V03 未交付的复杂表演、机甲及额外动作 | 未包含于首包和交付承诺 | 新动作交付前逐项视觉检查；不要求先完成全视频逐帧规格才验证窗口 |
| QAR05 的另一种导出倍率、W09 热加载 | 契约只支持一种倍率或无热加载功能 | 新增该能力或改加载链路；现用倍率/DPI和启动坏素材仍必做 |
| W12 专门性能预算/长期测量 | 诊断早期没有卡顿/增长信号，无性能承诺 | 出现性能问题、相关机制改变或发布范围含性能承诺；不以 100% 覆盖率或穷举作为目标 |

每次修复至少重跑失败输入与受影响相邻状态；通过且没有新变化/风险时不反复跑全套。低影响文案做人工核对；未知/模拟含义、可达退出不能因属于文字界面而省略行为检查。

## 统一实测回报模板

下方仅为待填写模板，不是已执行记录；人工操作不伪造退出码。证据不得包含对话全文、凭据或私人桌面内容。

```text
阶段 / 交付能力：<例如诊断窗口；明确独立、模拟或真实>
用例 ID / 分类：<原 ID；必做或本次触发原因>
结论：<待验 / 通过 / 失败 / 经 PM 接受延后或范围外>
实现提交 / 契约提交 / 素材版本：<完整哈希或无素材>
包路径与 SHA-256：<确切构建产物；不适用则说明>
环境：<Windows 版本与架构；SDK/运行时；干净环境有无开发工具>
显示环境：<显示器布局、分辨率、各 DPI、负坐标、实际设备>
事件来源与时间：<模拟/真实及版本；可控时钟输入；无事件则注明>
输入与操作：<可复现序列、初始状态、所选任务/轮次、边界数值>
命令 / 工作目录 / 退出码：<逐条原命令与实际退出码；人工检查填“不适用”>
预期：<引用接受契约，写出本次可判定值或行为>
实际：<观察值、状态、提示启动次数或错误类别，不能只写“正常”>
自动化输出：<报告/日志路径；没有则说明>
人工证据：<录屏/截图/键盘与讲述人记录，含操作时间点和文件位置>
执行人 / 复核人 / 日期：<角色及时间；未复核注明>
缺陷或待验原因 / 阻塞阶段：<缺设备、契约未定、失败复现链接等>
复用证据 / 省略理由：<原提交、未受影响范围，不能用于豁免首次必要检查>
下一步 / PM 接受记录：<修复后重测、限定范围或待决，不自行宣告豁免>
```

## 待集成与实际文档检查

本轮文档可交付，不等待其他 002 产物；以下三组影响后续工程门槛，交 PM 收齐后统一处理，不再询问已确定的产品方向：

1. DEV/ART-002 的宿主、坐标/DPI、最小素材与退出契约：未接受前不能给诊断加载/换算的精确预期；接受后映射第 1–3、5 节。
2. INT/DEV-002 的单任务模拟契约、提示消费、release 与新鲜度边界：未接受前第 4 节仅为用例设计；接受后填具体阈值与唯一责任。
3. 实机支持表与真实来源证据：缺设备阻塞相应 DPI/拓扑支持声明；缺受支持仅状态来源只阻塞第 6 节及最终包的真实联动声明，不能被模拟结果替代。

实际已读取 PM 决策文件及固定专业输入的既有内容/001 补充差异，并复用 QA 原矩阵与评审。执行 `git fetch origin`、`git show <提交>:<路径>`、`git diff <首稿> <固定专业提交> -- <路径>`；批量输出截断的 DEV 尾部另行补读。本轮不重复原作者运行调查，不声称覆盖尚未提交的 002 新规格。提交前只做允许路径、空白/Markdown、引用 ID 与模板一致性核查；实际命令和结果随交付报告，不建立文档镜像测试。既有 Draft PR 随原分支推送更新，不复制对话任务卡，不合并、不关闭 Issue，交付后等待 PM。
