# Windows 桌宠运行方案提案

状态：待统筹评审；不是已接受的 ADR 或共享接口。调查日期：2026-09-16。
关联：[Issue #1](https://github.com/Tc2006415/aemeath-desktop-pet/issues/1)。基线提交：`369b826`。

## 结论与实施门槛

建议在统筹接受独立桌宠方向后，优先验证 **C# / WPF / .NET**，备选 **TypeScript / Electron**。这是根据 Windows 单平台、透明小窗口和交互需求作出的工程判断，尚无性能实测。当前只完成只读调查和本提案，没有安装 SDK、创建运行时工程、确定依赖版本或修改 Codex。

WPF 的窗口和输入路径较贴近 Windows，适合本项目的有限 UI；Electron 的 Canvas 动画表达方便，但带入浏览器运行时，并有透明窗口、拖动区域及点击穿透限制。两者都需要原型实测，不能仅凭 API 文档认定满足需求。ADR 0001 仍由统筹接受；内置兼容皮肤的槽位限制沿用仓库调查，未在本任务重新核验，也不把独立桌宠推荐当作用户已选定方向。

开始实现前需要：统筹确认运行方式和候选技术栈；动画、联动、测试提案汇合；确定共享素材与状态语义。随后只实现一个可丢弃的 Windows 验证样机，再决定是否进入 M2。不得因提案合并自动安装工具链、接入私有状态源或公开发布。

## 实际证据与边界

已读取 AGENTS.md、README.md、开发流程、任务简报、ADR 0001 和参考笔记。通过已登录 GitHub 浏览器读取 Issue #1：所有权仅本文件，验收要求窗口/动画/交互、多显示器/DPI、接口需求、证据和 Draft PR。

本次命令及观察如下；“未发现”仅限 PATH 与列出的常见路径，不证明整机没有其他安装。

| 调查 | 实际结果 | 含义 |
| --- | --- | --- |
| `git status --short`；`git worktree list` | 初始工作区干净；当前独立 worktree 是 `C:/Users/bigxi/.codex/worktrees/e114/桌宠`，初始 detached HEAD | 使用现有隔离目录，不复用统筹主工作区 |
| `git switch -c codex/desktop-runtime-proposal` | 成功创建并切换 | 本任务独立分支 |
| `Get-Command gh,dotnet,node,npm,git,rustc,cargo -ErrorAction SilentlyContinue` | 只返回 node、git；该探测有缺项，不能视为全工具链检查通过 | 未发现 gh、.NET SDK、npm、Rust/Cargo |
| `node --version`；`git --version` | `v24.19.0`；`2.52.0.windows.1` | Node 来自 Codex 缓存运行时，不是项目专用安装 |
| `Test-Path` 检查下表路径 | 全部 False | 不执行缺失 SDK 的 build/publish 命令 |
| `Get-ChildItem <Node所在目录> -Name` | 只有 `node.exe` | 同目录没有 npm；Node 可执行不等于 Electron 构建链可用 |
| `Get-CimInstance Win32_OperatingSystem`，选取 Caption/Version/OSArchitecture | Windows 11 家庭版 中文版；`10.0.26200`；64 位 | 只确认本机 OS，不承诺所有 Windows 版本兼容 |
| `git ls-remote --heads origin` | 成功读取 main，指向 `369b826906417f6667917a78791dfeff3b6e6f2d` | 远程读取可用，不能单独证明推送权限 |

常见路径探测范围：

- `C:/Program Files/dotnet/dotnet.exe`
- `C:/Program Files (x86)/dotnet/dotnet.exe`
- `$env:USERPROFILE/.dotnet/dotnet.exe`
- `C:/Program Files/nodejs/node.exe`、`C:/Program Files/nodejs/npm.cmd`
- `$env:USERPROFILE/.cargo/bin/rustc.exe`
- `C:/Program Files/GitHub CLI/gh.exe`
- `C:/Program Files (x86)/Windows Kits/10/Include`

已视觉查看 `assets/concepts/aemeath-game-style-v2.png`：它是同画布上的三种造型/表情预览，背景呈灰色；只能作为粉发、蓝色头饰、紧凑比例及侧边装饰的外观参考。没有核验独立透明帧、统一网格、基线或时间信息，不能直接作为已验收图集加载。本任务未播放参考视频，不新增“已观察动作”结论；参考笔记的抽样位置仍不构成逐帧动画规格。

没有读取对话内容、凭据、Cookies 或账号数据库；Git 使用既有认证流程，本任务没有提取令牌。没有调查 Codex 私有运行时接口，真实来源证据属于联动任务。

## 两种技术栈比较

| 项目 | C# / WPF / .NET（优先验证） | TypeScript / Electron（备选） |
| --- | --- | --- |
| 透明窗口 | 官方 `AllowsTransparency` 配合 `WindowStyle=None`；窗口背景透明 [S1] | 无边框 `BrowserWindow` 配合 `transparent`，官方列出平台限制 [S2] |
| 动画 | 解码并缓存 PNG，裁切图集或切换帧，单调时钟决定帧索引；像素采样方式需实测 | Canvas 渲染帧，单调时间计算播放位置；关闭平滑并验证设备像素映射 |
| 拖动反馈 | 可评估 `DragMove`；调用时左键必须按下 [S3]。拖动循环内动画和状态消息能否及时处理须验证 | 原生 CSS 拖动区忽略指针事件 [S4]；专属拎起/松手反馈可能需要自管拖动和受限 IPC |
| 显示器 / DPI | 明确 DPI awareness、DIP 与 Win32 屏幕坐标转换，处理工作区和 DPI 变化；不能只设置缩放属性 [S5] | `screen` 提供显示器及工作区、变化事件和坐标转换；区分 DIP 与物理像素 [S6] |
| 工具链缺口 | 需要项目选定的 .NET SDK；本机未发现 | 需要可维护的 Node/npm 开发安装及 Electron 依赖；现有缓存 Node 不够 |
| 打包候选 | 先验证 `win-x64` 自包含目录发布；官方支持自包含与依赖运行时两类部署 [S7] | 后续选择官方生态打包工具并固定版本；打包携带 Electron 运行时 |
| 主要代价 | Windows 专属；托盘、显示器和特殊命中可能需要额外 Windows 互操作 | 进程、内存和分发体积成本需要实测；主进程/渲染进程边界和 IPC 校验增加维护量 |

本次没有 CPU、内存、启动时间、安装体积测量，不给出虚构数值或跨框架性能结论。SDK、Electron、打包器及第三方托盘库版本均不在本提案中冻结。

透明仅是绘制属性，不能等同于逐像素点击穿透。首个验证样机建议用紧凑矩形命中范围，单独记录透明边缘是否阻挡桌面；逐像素命中、点击穿透模式需要统筹明确纳入与否。不得直接全窗忽略鼠标而使桌宠无法再次抓取。Electron 的透明窗口不宜开启用户自由 resize，缩放验证应采用离散尺寸方案，并验证程序调整窗口或重建窗口是否稳定 [S2]。

## 内部职责与跨任务需求（均为建议）

程序内部划分四个职责，不在这里定义语言类型、JSON 字段或 IPC 通道：

1. **桌面宿主**：窗口、命中、拖动、工作区、DPI、托盘/设置、持久化和退出。
2. **状态协调**：接收被认可的业务状态，保存最新有效快照；将任务状态与临时交互状态合成为展示意图。
3. **动画播放器**：只认素材片段及播放请求；按单调时钟推进，报告一次性动作结束。不根据帧播完推断任务完成，不自行访问 Codex。
4. **输入适配边界**：真实来源和显式模拟器分别接入。模拟入口常驻显示“模拟”；无已验证来源时显示“未知/离线”，仍允许角色独立待机。

### 需要动画任务提供的语义

素材版本、透明 PNG 或图集布局、源帧尺寸与裁切范围、统一脚底/锚点、逐帧时长、循环或单次动作、动作中断/收尾规则、缺失动作回退、原始像素网格以及推荐展示倍率。拖动中姿态变化也须保持锚点，避免角色跳离鼠标。

程序不指定最终帧宽高、文件名、槽位、帧数或格式版本。内置皮肤的 192×208 和固定 6 帧不是独立桌宠规格。需要动画任务区分游戏观察动作与为桌宠原创的拎起、拖动、放下动作。素材路径限已批准的本地目录；加载器须拒绝越界裁切、非法时长、缺失/损坏图片并限制解码资源量，具体上限随素材规格共同确定。

### 需要联动任务提供的语义

能够区分 idle、working、waiting、completed、failed、unknown 的状态；任务身份、事件身份或可去重序号、产生时间/顺序依据、接收新鲜度、来源真实性和连接状态。需要明确快照与增量区别、断线/过期判定、终态有效期、恢复连接及多任务汇总策略。

这些是消费者需求清单，不是正式协议。不得把上述词汇直接实现为程序自创的外部 schema。多任务优先级、终态停留时长、允许的时间偏差、传输方式均由统筹结合联动提案决定。业务新鲜度与动画播放时钟分离，避免修改系统时间造成动画或过期逻辑异常。

### 拖动与任务变化的建议行为

拖动为临时视觉覆盖层，不替换业务状态。按下时进入拎起/拖动；期间继续接受合法业务更新；松手或失去鼠标捕获后结束覆盖，从**最新有效业务快照**重新选择动作，不能恢复按下时保存的旧动画。

例如 dragging 期间收到 completed，界面保持拖动姿态；松手时若完成状态仍新鲜且符合最终展示策略，显示完成动作一次；若已过期则显示未知/离线。重复终态不得重复触发庆祝，过时事件不得覆盖较新状态。去重与排序的责任归属必须在共享协议中唯一明确。拖动期间若依次发生 completed→working，应按最新 working 恢复，不补播旧庆祝。没有完成动画时允许中性姿态加文字状态，不能伪造角色动作已完成。

## 最小样机范围与验证计划

以下全部是 **未执行的后续验收计划**。实现须等 ADR 与接口接受；样机素材可用明确标记的诊断色块/测试帧，只检验渲染与交互，不作为角色成品交付。建议每次记录 OS、框架版本、显示器分辨率/缩放、步骤、实测现象和通过/失败，不收集其他应用内容。

| 编号 / 能力 | 最小方案与操作 | 通过标准 / 证据方式 |
| --- | --- | --- |
| R1 透明窗口 | 启动紧凑无边框窗口；在深浅桌面背景上检查四周，切换前台应用 | 无实色背景块、不抢键盘焦点；实际窗口截图/人工记录。置顶可关闭；不承诺覆盖安全桌面或独占全屏 |
| R2 动画 | 两个循环片段及一个单次测试片段；前后台切换、暂停后恢复、模拟低帧率 | 时间推进稳定、单次只结束一次、不会积压补播；虚拟时钟单测＋实际帧录制；正式角色仍需独立视觉验收 |
| R3 拖动 | 拎起、快速移动、窗口外释放、失去捕获、跨屏；同时送入模拟完成/失败/新工作事件 | 光标与锚点无明显跳变；所有结束路径均退出拖动并取最新有效状态，无永久卡住姿态；状态转换单测＋真实鼠标操作 |
| R4 缩放 | 初步比较 1×/2×/3×逻辑倍率，在 100%/125%/150%/200% 系统缩放下查看 | 最近邻采样无明显模糊、无遮挡、锚点稳定、命中随尺寸变化；若非整数物理像素造成不均匀，记录并由统筹选择逻辑尺寸或物理像素对齐策略 |
| R5 退出与恢复入口 | 托盘菜单含“设置/移回当前屏幕/退出”；设置窗口有键盘可达退出；测试托盘折叠及键盘导航 | 不依赖角色小面积右键才能退出；退出后进程/计时器关闭、无孤儿窗口；真实 Windows 操作 |
| R6 位置保存 | 松手及倍率变更后延迟合并保存；重启、写入失败、损坏配置 | 保存显示器参考与工作区相对位置、倍率及版本；临时文件原子替换候选方案；坏配置回退可见位置并可提示，不崩溃；文件读写/边界测试 |
| R7 多屏 | 副屏位于主屏左/上形成负坐标；跨不同 DPI；拖动中拔除屏幕、改主屏、变任务栏工作区 | 不把负数坐标当非法；窗口完整夹入仍存在的工作区；保存的屏幕不存在则移回当前可用屏幕；几何单测＋真实双屏操作 |
| R8 DPI | 在不同缩放屏间往返，停留后改变系统缩放 | 明确唯一坐标单位与转换边界；不重复乘缩放；重算尺寸后保留鼠标抓取偏移/脚底锚点，截图比对并记录误差；不能用浏览器设备模拟代替 |
| R9 状态异常 | 重复、乱序、断连、过期、重连及多任务事件，全部标注模拟 | 按最终共享协议验证；断连/过期不猜完成，模拟与真实来源可区分；假时钟/事件序列单测，真实联动另行验收 |
| R10 素材异常 | 坏 PNG、缺动作、越界帧、零/负时长、超大尺寸 | 可诊断地拒绝或回退，退出入口仍在；加载器边界测试，不以崩溃作为正常失败方式 |
| R11 打包 | ADR 后构建私有测试包，在未安装开发 SDK 的干净 Windows 用户环境启动、退出、重启 | 无需开发目录，资源路径正确，设置位于用户可写目录；记录包哈希/架构/日志。不设置开机启动、不自动更新、不公开上传 |

位置建议采用工作区内归一化的锚点位置，同时保留显示器参考作为提示；不能仅依赖可能变化的显示器编号。恢复时先匹配现有屏幕，再重新计算与夹取；倍率导致窗口大于工作区时限制显示尺寸或回退合适倍率。跨 DPI 时禁止把全局物理像素与局部 DIP 直接相减。最终存储 schema 和坐标约定在实现前由统筹确认。

R1/R3/R7/R8/R11 是技术选择的关键门槛。WPF 若无法在真实窗口验证透明渲染、拖动结束与 DPI 稳定性，提交失败证据后才申请改测 Electron；不同时搭建两个正式项目。性能基线在同一设备测冷启动、稳定待机和连续拖动的进程内存/CPU与包体积，阈值由统筹接受后再设为发布门槛。

## 交接与待决定事项

本阶段可依赖的是：工具链探测范围、官方 API 证据、程序所需语义和上述验证路径。不可依赖任何确定的素材 schema、状态协议、实际桌宠可运行或真实联动可用结论。自动化 CI、运行时单测、Windows 双屏/打包验证均尚未建立或执行。

统筹在汇总其他提案后需决定：

1. 是否接受独立桌宠方向及优先验证 WPF；同时明确首个目标 OS/架构和工具链安装方案。
2. 共同冻结素材语义、状态协议与责任归属，特别是拖动结束、终态停留、过期与多任务规则。
3. 是否要求逐像素点击穿透；缩放优先保持逻辑大小还是整倍物理像素；是否接受托盘＋可聚焦设置窗口的退出路径。
4. 确认最小样机的验证门槛，再授权 M2 工程实现；发布与合并仍由统筹决定。

## 官方资料

以下页面于 2026-09-16 查阅；它们证明 API 或限制的文档存在，不证明本机样机通过。

- [S1 WPF AllowsTransparency](https://learn.microsoft.com/en-us/dotnet/api/system.windows.window.allowstransparency?view=windowsdesktop-10.0)
- [S2 Electron 自定义窗口样式与透明限制](https://www.electronjs.org/docs/latest/tutorial/custom-window-styles)
- [S3 WPF DragMove](https://learn.microsoft.com/en-us/dotnet/api/system.windows.window.dragmove?view=windowsdesktop-10.0)
- [S4 Electron 拖动区域与鼠标穿透](https://www.electronjs.org/docs/latest/tutorial/custom-window-interactions)
- [S5 Windows 高 DPI 桌面开发](https://learn.microsoft.com/en-us/windows/win32/hidpi/high-dpi-desktop-application-development-on-windows)
- [S6 Electron screen](https://www.electronjs.org/docs/latest/api/screen)
- [S7 .NET 部署概览](https://learn.microsoft.com/en-us/dotnet/core/deploying/)
