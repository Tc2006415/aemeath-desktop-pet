# ART026 待机初版交付（待用户审阅）

本轮五核心已输出到 `art026-core/`；自包含 `art026-demo.html` 可离线打开。A 保留用户选择的 ART023 第二版精确 PNG 字节，B/C 为新生成羽翼，两张笑脸保留现正式非翼像素并逐像素复用 B/C。尚未替换正式包，尚非原生桌宠验收；其他动作沿用第二版的后续范围由 PM 派单。

## 制作记录

共使用 3 次新 imagegen（上限4）；B/C 各1版合成（上限2）。第1次全画布源比例偏离，拒绝合成；第2次清晰双格源抬幅过大，仅转换 donor 检查；第3次双格源用于最终5核心。源图、提示词、放大参考、失败原因均保留。未用程序画羽、旋转或移动 A 制造姿态。

精确条带参考为 A 的 x0..95/y72..103，12倍整数放大后上下双格。选用源1536×1024，上格B `[0,0,1536,512)`，下格C `[0,512,1536,1024)`；各格像素中心近邻采样96×32，放回y72，alpha128。两格脚底均93，固定根部alpha差0，额外偏移(0,0)。语义mask838像素、固定根部32像素与ART023一致。详细 SHA 和坐标见 build-report。

## 实际验证

- Python `art026-build.py`：退出0；B/C分别186/224变化像素，mask外RGBA差0、固定根部差0；笑脸非翼保护、翼部精确复用通过。
- Python `art026-preview.py`：退出0，10个嵌入PNG原字节一致（新5+可选旧5）。
- Python `art026-check.py`：格式、CRC、A原字节、保护、复用、HTML实际嵌入字节、原始时序及接缝通过；结果另见check-report，透明小孔以发现项保留，不冒充零缺陷。
- PowerShell `scripts/qa/Inspect-Png.ps1` 对5核心集中执行：全为96×104、RGBA8、alpha仅0/255，半透明数0（png-inspect.json）。
- CUA 实际浏览器打开本地演示，观察A/B/C、半闭眼、闭眼笑的暂停帧及运行时不同阶段；深浅背景、1/2/3倍、旧版对照、上下逐帧、继续播放均核对。修正静帧选笑脸后继续播放应保持笑脸动作的预览状态问题，并实际复查。
- 原时序逐项一致：轻扇1400ms、笑脸1200ms；两动作首尾均精确A。连续演示三次轻扇加一次笑脸5400ms。运行截图采样不等于连续视频或原生应用实测。
- 正式包manifest和16PNG的已有SHA保持一致，仅作保护核对；未重跑旧11帧、旧16入口整套验收。

## 已知细节与交接

B/C无新增8邻域孤立碎块，三羽轮廓在3倍视图可辨。4邻域透明检查发现B `(22,88)/(73,88)`、C `(21,86)/(74,86)` 各两个对称单像素透明孔；相应笑脸复用同样翼部。这些细节未经修补，明确作为初版待用户反馈项；不是生产级零孔验收。B/C羽尖及明暗随生成有细微差异，整体动作与羽片长度感以用户审阅为准，不继续无限打磨。

旧动作依赖风险沿用ART022/024记录：neutral是共享入口，不能仅覆盖这5图后宣称全动作完成。请PM先展示该HTML收集用户反馈，再派下一阶段。不要整体合并本分支历史正式包（分支正式文件仍是旧版本）；本次只交接art026候选和本人进度记录，不合并PR、不发布。
