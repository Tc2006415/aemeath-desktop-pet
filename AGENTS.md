# 协作规则

## 必读

开始工作前读取 README.md、docs/development-workflow.md、docs/task-briefs.md，以及本任务对应的 Issue 和现行 ADR。参考视频的抽样观察不是已完成的逐帧动画规格。

现行任务卡由 PM 直接发送到对应任务对话，不再同步 GitHub。仓库 docs/task-cards/ 下内容是历史记录；最新对话任务卡优先。收到派单后按允许文件、禁止范围和验收条件执行。无任务卡不得自行进入下一阶段。跨模块变更先提交影响说明；任务卡不授权修改其他角色的文件。

## 分支与所有权

- 当前统筹任务维护产品范围、共享接口、ADR、集成分支和发布决定。
- 各开发任务使用自己的 worktree 和 codex/ 前缀分支。
- 单个任务只处理分配范围，不顺手修改其他模块或依赖锁文件。
- 公共接口或依赖变化先提交提案，由统筹任务接受后更新；其他任务不得自行创造互不兼容的接口。
- PR 合并由统筹任务执行。不得自动强推、重写他人提交或删除他人分支。

## 真实性与边界

- 不把模拟事件称为真实 Codex 联动，不把概念图称为动画完成。
- 不修改、注入或重打包 Codex 安装文件。只读调查不等于运行时接口获得支持。
- 不读取或上传完整对话内容、凭据、浏览器 Cookies、账号数据库。联动优先使用有文档的本地事件接口。
- 未验证的任务状态应显示未知或离线，不根据不可靠信号猜测工作已完成。
- 没有明确授权时，不新增自动开机启动或公开发布。

## 交付

- 交付包含：改动摘要、实际验证命令及结果、限制、PR 或提交哈希、交接说明。
- 测试覆盖状态转换、拖动结束恢复、过期事件等真实风险，避免只镜像实现的测试。
- 图片编辑和生成使用 imagegen 技能；不能用程序绘制的占位图冒充角色成品。
- 只有视觉检查后才能宣称像素素材可用；只有真实应用运行后才能宣称桌宠可运行。
- 用户当前明确要求以不同 Codex 任务分工；不要擅自额外创建任务。
