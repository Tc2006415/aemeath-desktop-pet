# DEV-001：程序架构与窗口方案

版本：1.0。签发：PM，2026-09-16。

- 责任任务：爱弥斯桌宠 · 程序架构
- 正式任务 ID：01a0abdf-e286-7e01-b893-937bd3ddd507
- Issue：https://github.com/Tc2006415/aemeath-desktop-pet/issues/1
- 分支：codex/desktop-runtime-proposal；独立 worktree，不切换 PM 工作区分支。
- 首稿基准：42090e824b9c57e4ad2f128d0a8e0fa5f8bae2ee，已经提交，待 PM 验收；不等于功能完成。
- 阶段：M1；本轮授权为文档补充/核查，不进入 M2 编码。

## 目标

提供 Windows 桌宠运行方式与窗口交互方案，给 PM 可评审的决策依据。

## 输入与依赖

阅读 AGENTS.md、docs/task-cards/README.md、docs/project-status.md 和 ADR 0001。交叉审阅时读取以下已提交版本，不需要合并其他分支：程序 42090e8、动画 c78a545、联动 9a01b91、测试 e96dd65。可使用 git show <commit>:<path>；不能将他人的未接受提案当作正式接口。

## 允许范围与文件所有权

只读检查工具链与官方文档；比较最多两种技术栈；提出透明窗口、拖动、缩放、退出、位置保存及 DPI 验证路径。

唯一允许修改的交付路径：docs/proposals/desktop-runtime.md。

## 禁止范围

不得安装工具链、创建运行时工程、改动素材、实现 Codex 适配器、接受 ADR 或修改依赖锁文件。 不得自行合并、公开发布、创建额外任务或修改本任务卡。

## 本轮具体任务

核对动画与联动已提交提案，在本文件新增“跨提案依赖核对”小节：列出一致项、冲突项、需要 PM 决策的选项及其影响。最多提出 5 个真正阻塞最小样机的问题，不重复询问已授权的调查。

## 验收条件

工具链结论有实际命令证据；推荐与实测分开；拖动恢复使用最新有效状态；跨提案问题精确引用文件小节。

文件差异必须在允许路径内；git diff --check 无错误；交付包含实际使用的来源或命令和未验证项。提交到上述分支，更新已有 Draft PR；无需重复创建 PR，也不关闭原 Issue。若查不到 PR，报告分支与提交即可。

## 交接格式

按“任务卡编号 / 状态 / 基准与交付提交 / 修改文件 / 验证证据 / 未验证及风险 / 待 PM 决定事项 / PR 链接”提交。状态只能是执行中、阻塞、待验收；完成由 PM 验收后标记。

## 越界与阻塞处理

需要改动其他文件或接口时，先提交变更请求，写明原因、影响、最小范围和备选方案；继续完成不依赖该决定的授权工作。不要让用户重复批准已有任务范围。交付后等待下一张任务卡，不自动进入下一里程碑。
