# QA-008：ART-018 完整视觉探针

## 结论

**完整素材和网页视觉可进入接口冻结讨论，本轮未发现需要先返工素材的阻断项。** 该判断仅针对固定ART-018探针的实际尺寸观感、来源及时序；不是可加载包或原生桌宠通过。B4/B7/高位的短羽形色差、不均步幅和高位每侧1px收窄保留为已披露限制，不要求每个羽像素绝对相同。

源帧选路、快松手的正确入口、重抓策略、真正位置冻结及鼠标捕获仍须DEV实现和原生验证。现有网页固定R回退、neutral重新pickup、坐标数值模拟均未记为真实输入通过。未启动旧宿主，没有以QA007旧控制器证据替代新路由验收；未改素材、正式包、生产源码、锁文件或契约，无发布、联动、自启、idle/float扩展。

## 固定输入与哈希澄清

任务卡提交 `5a61f0e096441caed5a2d0fff017b6dfbc337bfc`。开工工作树干净，未合并ART历史分支；使用 `git archive` 只导出source到 `artifacts/qa008-art-5a61f0e/assets/characters/aemeath-v1/source`。指定原路径为 `C:/Users/bigxi/.codex/worktrees/eaf8/桌宠/assets/characters/aemeath-v1/source/art018-inspection.html`，与运行快照逐字节一致，12张引用PNG也与指定来源一致。

初查发现任务卡“HTML SHA”与现场原始文件不同，已通知PM；PM确认是换行格式而非另一视觉版本，QA随后独立调用git show取二进制blob验证：

- 现场/运行归档原始字节149433，CRLF，SHA-256 `9b4232ebd6c6d170f1c24bc63c2e4280f5170672d77ce3f271bfb09c4936486a`。
- Git blob149379字节，LF，SHA-256 `f53b38a87ff642842638a01621ac06de74fb9383da69ec106453b5d2a4c8f4a2`。
- 仅将现场CRLF规范为LF后与实际git blob逐字节一致。未修改或重制ART文件。

## 独立文件验证

实际命令 `python -B scripts/qa/check_art018.py` 退出0；复用QA自有 `png_readonly.py` 读PNG块、CRC及逆过滤，不执行ART检查/生成脚本。结果为 `artifacts/qa008-check.json`，命令输出另存 `artifacts/qa008-command-output.json`。

12张内嵌PNG逐字节等于来源文件且SHA与固定清单一致，全部静态96×104 RGBA8、二值alpha、CRC通过、非全透明且不触画布边；冠顶均y22。所有帧上部静止区（y<75，排除既有222像素表情mask）对neutral差分0；B2/B4/B7/up/down在翼mask之外相对mid差分0。

新B7确实绑定 `art017-v1-96.png`，SHA `8cac356f170275806966d31364520c3bbc5ad3a66e04160ed59814dd4ab9557b`，不是旧ART015 B7。其1060像素mask独立核对为原翼mask的指定子集；相对旧B7变化278像素、mask外RGBA变化0，mask内等于ART017 donor。未把mask外不变扩写成所有运动像素都不变。

时序内嵌数据与固定来源清单相同。相对ART016只有B7图片绑定变化，以及hold零基8/9/18/19时长变为100/140/100/140ms；其余pickup、release、routes数组不变。hold20项1440ms，两次720ms翼周期；仅零基18项闭眼100ms。格式/时序断言与下节视觉判定分开。

## 实际浏览器观察

CUA实际URL：`http://127.0.0.1:8878/art018-inspection.html`。临时Python服务器绑定127.0.0.1，服务上述QA快照目录；未修改页面CSS、图片、时钟或源码。四格同时显示浅底3×、深底3×、浅底1×、深底1×。工具截图是播放抽样和逐项对照，不是每个显示刷新帧的录屏，不据DOM计数单独宣称视觉通过。

| 观察 | 实際证据与判断 |
| --- | --- |
| 连续扇翼 | 重播后保持运行未暂停，截图约273ms为up、401ms为B4、516ms为mid，后续同次连续运行约6394ms为down、6503ms为mid、6606ms为B2，已跨过至少5翼周期。实看上扬/下压/中位四格，头饰和身体稳定，未见整只角色跳位或翼根断裂；不把中间未截图的每一刷新帧称为已逐帧检查 |
| 闭眼100ms可读性 | 定位1050ms暂停后恢复正常速度；实际截图1270ms为closed、1373ms为down恢复睁眼、1470ms为mid。1×能辨认短促闭眼，3×眼线与嘴部表情清楚；更像短眨眼而非长闭眼保持。没有用暂停图替代这段实际播放观察 |
| B4/B7/高位短羽与宽度 | 分别定位140ms B4、190ms新B7、260ms up实看四格。B7短羽与右侧轮廓可辨，B4→B7更接近平展，up再上扬；3×能看到白色填充及短羽形状变化、步幅不均。高位范围(9…86)相较B4/B7(8…87)每侧内收1源像素；1×未显为大幅横向跳宽。未观察到孤立残影/透明洞或需要阻断的整体倒序，不声称形色差完全消除 |
| 上扬松手入口 | 选择“上扬松手”正常播放，截图约193/277/362ms为源up，441ms为release的B7。大幅上扬保留；向中间级收翼的方向明确。40ms中间级很短，当前检查有逐项对照补充，未要求缩减已选幅度 |
| 下压松手入口与落稳 | 定位410ms处源down，恢复播放后截图622/707ms为rhalf落稳，786ms为neutral。下压→R动作较快，身体落稳变化可辨；未见冠饰漂移或明显边缘破损。坐标栏“冻结”只证明网页数值模拟显示，不证明原生位置停止 |

上扬与下压的路径时序采用页面既有设计：上扬源40ms后经剩余B级各40ms再R460ms；下压/中位源40ms后直接R。本轮没有强求不同入口时间完全相等，也未把不均步幅自动判为缺陷。是否采纳具体数组属于PM接口冻结决定，QA结论是没有发现视觉上必须先返工的阻断。

## 必须保留给DEV与原生验证的项目

1. 按实际当前源帧选择收翼路由，并明确帧ID、索引、时间边界和版本约束；ART-018目前只是网页数组，不能直接宣称生产加载器支持。
2. 快松手：页面40ms情境固定R，pickup源帧选路未实现；DEV需按冻结设计处理，特别覆盖首次pickup阶段与松手边界。
3. 重抓：页面立即从neutral重新pickup，没有当前姿态过渡。此为已知设计/实现缺口，需冻结入口策略，再测旧完成隔离与实际跳变，不能据网页按钮当作真实捕获通过。
4. 真实松手后位置冻结、鼠标捕获丢失/失焦/取消、release期间重抓及正常退出，均需生产宿主和原生输入证据。本轮未运行宿主或模拟OS输入。
5. QA007旧控制器的基本恢复/旧实例隔离证据可复用作未改部分参考；新增路由后的受影响边界必须定向复验，不必重跑无关完整套件。

这五项属于进入实现前后必须解决的接口和运行风险，不因素材/网页观感通过而消失。接口冻结不等同产品完成。

## 交接

提交只含 `scripts/qa/check_art018.py` 和本报告；提交前 `git diff --cached --check` 通过、路径检查无越权。像素清单与截图工具输出为实际证据；临时检查页/服务器验后关闭。可从QA根目录执行 `python -m http.server 8878 --bind 127.0.0.1 --directory artifacts/qa008-art-5a61f0e/assets/characters/aemeath-v1/source` 重现URL。回报PM后停止等待，不修图、不推广正式包或进入后续实现。
